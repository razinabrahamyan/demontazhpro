/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./public/assets/js/scripts.js":
/*!*************************************!*\
  !*** ./public/assets/js/scripts.js ***!
  \*************************************/
/***/ (() => {

$(document).ready(function () {
  /*PRELOADER*/
  $(".loader").delay(1000).fadeOut(500);
  /*POPUP OPEN*/

  $(document).on('click', '.open-popup-form', function () {
    $('#priceModal').modal('show');
  });
  setTimeout(function () {
    $('#priceModal').modal('show');
  }, 60 * 1000);
  /*===================================*
  COOKIES
  /*===================================*/

  window.getCookie = function (name) {
    var value = "; ".concat(document.cookie);
    var parts = value.split("; ".concat(name, "="));
    if (parts.length === 2) return parts.pop().split(';').shift();
  };
  /*===================================*
  PWA
  /*===================================*/


  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('https://demontazh-pro.ru/serviceworker.js').then(function (registration) {// console.log('PWA: ServiceWorker registration successful with scope: ', registration.scope);
    }, function (err) {
      console.log('PWA: ServiceWorker registration failed: ', err);
    });
  } else {
    console.log('ServiceWorker Not Found');
  }
});
/*===================================*
MAPPING
/*===================================*/

window.yaMapsShown = false;
window.map = undefined;

window.initMap = function () {
  window.map = new ymaps.Map("map", {
    center: [55.587041, 37.739923],
    zoom: 11,
    controls: []
  }, {
    searchControlProvider: 'yandex#search',
    suppressMapOpenBlock: true,
    suppressObsoleteBrowserNotifier: true
  });
  window.map.controls.add(new ymaps.control.FullscreenControl());
  window.map.controls.add(new ymaps.control.ZoomControl({
    options: {
      size: "small"
    }
  }));
  this.map.geoObjects.add(new ymaps.Placemark([55.562999, 37.753946], {
    balloonContent: 'Проектируемый проезд № 5208\n' + 'Видное, Ленинский городской округ, Московская область, Россия'
  }));
  this.map.geoObjects.add(new ymaps.Placemark([55.555560, 37.711096], {
    balloonContent: 'улица Павла Фёдоровича Гаевского, 3\n' + 'Видное, Ленинский городской округ, Московская область, Россия, 142703'
  }));
  this.map.geoObjects.add(new ymaps.Placemark([55.544204, 37.720034], {
    balloonContent: 'Берёзовая улица\n' + 'Видное, Ленинский городской округ, Московская область, Россия'
  }));
  this.map.geoObjects.add(new ymaps.Placemark([55.550366, 37.692878], {
    balloonContent: '1-й микрорайон\n' + 'Видное, Ленинский городской округ, Московская область, Россия'
  }));
};

window.mountMap = function () {
  var target = $('#map');
  var el = target[0];
  var rect = el.getBoundingClientRect();

  if (rect.top >= 0 && rect.left >= 0 && rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
  /* or $(window).height() */
  rect.right <= (window.innerWidth || document.documentElement.clientWidth)
  /* or $(window).width() */
  ) {
    var elem = document.createElement('script');
    elem.type = 'text/javascript';
    elem.src = 'https://api-maps.yandex.ru/2.1/?load=package.standard&lang=ru_RU&amp;apikey=8c1ddd83-7c9c-4d9e-a532-9c67ea915ff8&onload=window.initMap';
    document.getElementsByTagName('body')[0].appendChild(elem);
    window.yaMapsShown = true;
  }
}; //


if ($('#map').is(":visible")) {
  $(window).scroll(function () {
    if (!window.yaMapsShown) {
      window.mountMap();
    }
  });
}
/*===================================*
OWL
/*===================================*/


var owlMetalCards = $('.owl-metal-cards');

if (owlMetalCards.length) {
  owlMetalCards.owlCarousel({
    loop: true,
    margin: 0,
    responsiveClass: true,
    dots: false,
    stagePadding: Number,
    //stagePadding: 50,
    rtl: true,
    pagination: false,
    responsive: {
      0: {
        items: 2
      },
      650: {
        items: 3
      },
      800: {
        items: 4
      },
      1200: {
        items: 5
      }
    }
  });
  $("#owl-metal-cards-wrapper .next").click(function () {
    owlMetalCards.trigger('prev.owl.carousel');
  });
  $("#owl-metal-cards-wrapper .prev").click(function () {
    owlMetalCards.trigger('next.owl.carousel');
  });
  owlMetalCards.trigger('owl.play', false);
}
/*===================================*
11. SCROLLUP JS
*===================================*/


$(window).scroll(function () {
  if ($(this).scrollTop() > 150) {
    $('.scrollup').addClass('pptItemVisible');
  } else {
    $('.scrollup').removeClass('pptItemVisible');
  }
});
$(".scrollup").on('click', function (e) {
  e.preventDefault();
  window.smoothScroll(0);
  return false;
});

window.scrollToElement = function (element) {
  window.smoothScroll($(element).offset().top);
};

window.smoothScroll = function (to, id) {
  var smoothScrollFeature = ('scrollBehavior' in document.documentElement.style);
  var articles = document.querySelectorAll("ul#content > li"),
      i;
  if (to == 'elem') to = articles[id].offsetTop;
  var i = parseInt(window.pageYOffset);

  if (i != to) {
    if (!smoothScrollFeature) {
      to = parseInt(to);

      if (i < to) {
        var _int = setInterval(function () {
          if (i > to - 20) i += 1;else if (i > to - 40) i += 3;else if (i > to - 80) i += 8;else if (i > to - 160) i += 18;else if (i > to - 200) i += 24;else if (i > to - 300) i += 40;else i += 60;
          window.scroll(0, i);
          if (i >= to) clearInterval(_int);
        }, 1);
      } else {
        var _int = setInterval(function () {
          if (i < to + 20) i -= 1;else if (i < to + 40) i -= 3;else if (i < to + 80) i -= 8;else if (i < to + 160) i -= 18;else if (i < to + 200) i -= 24;else if (i < to + 300) i -= 40;else i -= 60;
          window.scroll(0, i);
          if (i <= to) clearInterval(_int);
        }, 15);
      }
    } else {
      window.scroll({
        top: to,
        left: 0,
        behavior: 'smooth'
      });
    }
  }
};
/*VUE JS*/


var MaskedInput = window.vueTextMask["default"];
window.popupForm = new Vue({
  el: "#priceModal",
  components: {
    MaskedInput: MaskedInput
  },
  data: {
    formData: undefined,
    formSent: false,
    phone: '',
    full_name: '',
    mask: ['+', '7', '(', /\d/, /\d/, /\d/, ')', '-', /\d/, /\d/, /\d/, '-', /\d/, /\d/, '-', /\d/, /\d/],
    success: false,
    minutes: 0,
    seconds: 0,
    milliseconds: 0
  },
  methods: {
    sendForm: function sendForm(e) {
      e.preventDefault();
      window.popupForm.success = true;
      window.popupForm.countdownTimer();

      if (window.getCookie("FORM_HAS_SENDED") !== "1") {
        var config = {
          headers: {
            Accept: 'application/json',
            'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
          }
        };
        var formData = new FormData(e.target); // formData.append('page', $('h1').text());

        formData.append('link', window.location.href);
        axios.post('/axios/send-form', formData, config).then(function (response) {
          if (response.status !== 200) {} else {
            var date = new Date();
            date.setTime(date.getTime() + 3 * 60 * 1000);
            expires = "; expires=" + date.toUTCString();
            document.cookie = 'FORM_HAS_SENDED' + "=1" + expires + "; path=/";
          }
        })["catch"](function (e) {
          console.log(e);
        });
      }
    },
    countdownTimer: function Countdown() {
      var that = {};
      var seconds = 180;
      that.seconds = seconds;
      that.totalTime = seconds * 100;
      that.usedTime = 0;
      that.startTime = +new Date();
      that.timer = null;
      that.usedTime = Math.floor((+new Date() - that.startTime) / 10);

      that.count = function () {
        that.usedTime = Math.floor((+new Date() - that.startTime) / 10);
        var tt = that.totalTime - that.usedTime;

        if (tt > 0) {
          window.popupForm.minutes = window.popupForm.fillZero(Math.floor(tt / (60 * 100)));
          window.popupForm.seconds = window.popupForm.fillZero(Math.floor((tt - window.popupForm.minutes * 60 * 100) / 100));
          window.popupForm.milliseconds = window.popupForm.fillZero(tt - Math.floor(tt / 100) * 100);
        }
      };

      setInterval(that.count, 1);
    },
    fillZero: function fillZero(num) {
      return num < 10 ? '0' + num : num;
    }
  }
});
window.form1 = new Vue({
  el: "#call-to-action-one",
  components: {
    MaskedInput: MaskedInput
  },
  data: {
    formData: undefined,
    formSent: false,
    phone: '',
    full_name: '',
    mask: ['+', '7', '(', /\d/, /\d/, /\d/, ')', '-', /\d/, /\d/, /\d/, '-', /\d/, /\d/, '-', /\d/, /\d/],
    success: false,
    minutes: 0,
    seconds: 0,
    milliseconds: 0
  },
  methods: {
    sendForm: function sendForm(e) {
      e.preventDefault();
      window.form1.success = true;
      window.form1.countdownTimer();

      if (window.getCookie("FORM_HAS_SENDED") !== "1") {
        var config = {
          headers: {
            Accept: 'application/json',
            'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
          }
        };
        var formData = new FormData(e.target); // formData.append('page', $('h1').text());

        formData.append('link', window.location.href);
        axios.post('/axios/send-form', formData, config).then(function (response) {
          if (response.status !== 200) {} else {
            var date = new Date();
            date.setTime(date.getTime() + 3 * 60 * 1000);
            expires = "; expires=" + date.toUTCString();
            document.cookie = 'FORM_HAS_SENDED' + "=1" + expires + "; path=/";
          }
        })["catch"](function (e) {
          console.log(e);
        });
      }
    },
    countdownTimer: function Countdown() {
      var that = {};
      var seconds = 180;
      that.seconds = seconds;
      that.totalTime = seconds * 100;
      that.usedTime = 0;
      that.startTime = +new Date();
      that.timer = null;
      that.usedTime = Math.floor((+new Date() - that.startTime) / 10);

      that.count = function () {
        that.usedTime = Math.floor((+new Date() - that.startTime) / 10);
        var tt = that.totalTime - that.usedTime;

        if (tt > 0) {
          window.form1.minutes = window.form1.fillZero(Math.floor(tt / (60 * 100)));
          window.form1.seconds = window.form1.fillZero(Math.floor((tt - window.form1.minutes * 60 * 100) / 100));
          window.form1.milliseconds = window.form1.fillZero(tt - Math.floor(tt / 100) * 100);
        }
      };

      setInterval(that.count, 1);
    },
    fillZero: function fillZero(num) {
      return num < 10 ? '0' + num : num;
    }
  }
});
window.form2 = new Vue({
  el: "#form_2",
  components: {
    MaskedInput: MaskedInput
  },
  data: {
    formData: undefined,
    formSent: false,
    phone: '',
    full_name: '',
    mask: ['+', '7', '(', /\d/, /\d/, /\d/, ')', '-', /\d/, /\d/, /\d/, '-', /\d/, /\d/, '-', /\d/, /\d/],
    success: false,
    minutes: 0,
    seconds: 0,
    milliseconds: 0
  },
  methods: {
    sendForm: function sendForm(e) {
      e.preventDefault();
      window.form2.success = true;
      window.form2.countdownTimer();

      if (window.getCookie("FORM_HAS_SENDED") !== "1") {
        var config = {
          headers: {
            Accept: 'application/json',
            'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
          }
        };
        var formData = new FormData(e.target); // formData.append('page', $('h1').text());

        formData.append('link', window.location.href);
        axios.post('/axios/send-form', formData, config).then(function (response) {
          if (response.status !== 200) {} else {
            var date = new Date();
            date.setTime(date.getTime() + 3 * 60 * 1000);
            expires = "; expires=" + date.toUTCString();
            document.cookie = 'FORM_HAS_SENDED' + "=1" + expires + "; path=/";
          }
        })["catch"](function (e) {
          console.log(e);
        });
      }
    },
    countdownTimer: function Countdown() {
      var that = {};
      var seconds = 180;
      that.seconds = seconds;
      that.totalTime = seconds * 100;
      that.usedTime = 0;
      that.startTime = +new Date();
      that.timer = null;
      that.usedTime = Math.floor((+new Date() - that.startTime) / 10);

      that.count = function () {
        that.usedTime = Math.floor((+new Date() - that.startTime) / 10);
        var tt = that.totalTime - that.usedTime;

        if (tt > 0) {
          window.form2.minutes = window.form2.fillZero(Math.floor(tt / (60 * 100)));
          window.form2.seconds = window.form2.fillZero(Math.floor((tt - window.form2.minutes * 60 * 100) / 100));
          window.form2.milliseconds = window.form2.fillZero(tt - Math.floor(tt / 100) * 100);
        }
      };

      setInterval(that.count, 1);
    },
    fillZero: function fillZero(num) {
      return num < 10 ? '0' + num : num;
    }
  }
});
window.headSection = new Vue({
  el: "#head-section",
  components: {
    MaskedInput: MaskedInput
  },
  data: {
    formData: undefined,
    formSent: false,
    phone: '',
    full_name: '',
    mask: ['+', '7', '(', /\d/, /\d/, /\d/, ')', '-', /\d/, /\d/, /\d/, '-', /\d/, /\d/, '-', /\d/, /\d/],
    success: false,
    minutes: 0,
    seconds: 0,
    milliseconds: 0
  },
  methods: {
    sendForm: function sendForm(e) {
      e.preventDefault();
      window.headSection.success = true;
      window.headSection.countdownTimer();

      if (window.getCookie("FORM_HAS_SENDED") !== "1") {
        var config = {
          headers: {
            Accept: 'application/json',
            'X-CSRF-TOKEN': $('meta[name="csrf-token"]').attr('content')
          }
        };
        var formData = new FormData(e.target); // formData.append('page', $('h1').text());

        formData.append('link', window.location.href);
        axios.post('/axios/send-form', formData, config).then(function (response) {
          if (response.status !== 200) {} else {
            var date = new Date();
            date.setTime(date.getTime() + 3 * 60 * 1000);
            expires = "; expires=" + date.toUTCString();
            document.cookie = 'FORM_HAS_SENDED' + "=1" + expires + "; path=/";
          }
        })["catch"](function (e) {
          console.log(e);
        });
      }
    },
    countdownTimer: function Countdown() {
      var that = {};
      var seconds = 180;
      that.seconds = seconds;
      that.totalTime = seconds * 100;
      that.usedTime = 0;
      that.startTime = +new Date();
      that.timer = null;
      that.usedTime = Math.floor((+new Date() - that.startTime) / 10);

      that.count = function () {
        that.usedTime = Math.floor((+new Date() - that.startTime) / 10);
        var tt = that.totalTime - that.usedTime;

        if (tt > 0) {
          window.headSection.minutes = window.headSection.fillZero(Math.floor(tt / (60 * 100)));
          window.headSection.seconds = window.headSection.fillZero(Math.floor((tt - window.headSection.minutes * 60 * 100) / 100));
          window.headSection.milliseconds = window.headSection.fillZero(tt - Math.floor(tt / 100) * 100);
        }
      };

      setInterval(that.count, 1);
    },
    fillZero: function fillZero(num) {
      return num < 10 ? '0' + num : num;
    }
  }
});
/*VUE JS*/

/***/ }),

/***/ "./resources/css/style.scss":
/*!**********************************!*\
  !*** ./resources/css/style.scss ***!
  \**********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "./public/assets/bootstrap/css/bootstrap.min.css":
/*!*******************************************************!*\
  !*** ./public/assets/bootstrap/css/bootstrap.min.css ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "./public/assets/fontawesome/css/all.min.css":
/*!***************************************************!*\
  !*** ./public/assets/fontawesome/css/all.min.css ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = __webpack_modules__;
/******/
/************************************************************************/
/******/ 	/* webpack/runtime/chunk loaded */
/******/ 	(() => {
/******/ 		var deferred = [];
/******/ 		__webpack_require__.O = (result, chunkIds, fn, priority) => {
/******/ 			if(chunkIds) {
/******/ 				priority = priority || 0;
/******/ 				for(var i = deferred.length; i > 0 && deferred[i - 1][2] > priority; i--) deferred[i] = deferred[i - 1];
/******/ 				deferred[i] = [chunkIds, fn, priority];
/******/ 				return;
/******/ 			}
/******/ 			var notFulfilled = Infinity;
/******/ 			for (var i = 0; i < deferred.length; i++) {
/******/ 				var [chunkIds, fn, priority] = deferred[i];
/******/ 				var fulfilled = true;
/******/ 				for (var j = 0; j < chunkIds.length; j++) {
/******/ 					if ((priority & 1 === 0 || notFulfilled >= priority) && Object.keys(__webpack_require__.O).every((key) => (__webpack_require__.O[key](chunkIds[j])))) {
/******/ 						chunkIds.splice(j--, 1);
/******/ 					} else {
/******/ 						fulfilled = false;
/******/ 						if(priority < notFulfilled) notFulfilled = priority;
/******/ 					}
/******/ 				}
/******/ 				if(fulfilled) {
/******/ 					deferred.splice(i--, 1)
/******/ 					var r = fn();
/******/ 					if (r !== undefined) result = r;
/******/ 				}
/******/ 			}
/******/ 			return result;
/******/ 		};
/******/ 	})();
/******/
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/
/******/ 	/* webpack/runtime/jsonp chunk loading */
/******/ 	(() => {
/******/ 		// no baseURI
/******/
/******/ 		// object to store loaded and loading chunks
/******/ 		// undefined = chunk not loaded, null = chunk preloaded/prefetched
/******/ 		// [resolve, reject, Promise] = chunk loading, 0 = chunk loaded
/******/ 		var installedChunks = {
/******/ 			"/assets/js/scripts.min": 0,
/******/ 			"assets/css/style": 0,
/******/ 			"assets/fontawesome/css/all-purge.min": 0,
/******/ 			"assets/bootstrap/css/bootstrap-purge.min": 0
/******/ 		};
/******/
/******/ 		// no chunk on demand loading
/******/
/******/ 		// no prefetching
/******/
/******/ 		// no preloaded
/******/
/******/ 		// no HMR
/******/
/******/ 		// no HMR manifest
/******/
/******/ 		__webpack_require__.O.j = (chunkId) => (installedChunks[chunkId] === 0);
/******/
/******/ 		// install a JSONP callback for chunk loading
/******/ 		var webpackJsonpCallback = (parentChunkLoadingFunction, data) => {
/******/ 			var [chunkIds, moreModules, runtime] = data;
/******/ 			// add "moreModules" to the modules object,
/******/ 			// then flag all "chunkIds" as loaded and fire callback
/******/ 			var moduleId, chunkId, i = 0;
/******/ 			if(chunkIds.some((id) => (installedChunks[id] !== 0))) {
/******/ 				for(moduleId in moreModules) {
/******/ 					if(__webpack_require__.o(moreModules, moduleId)) {
/******/ 						__webpack_require__.m[moduleId] = moreModules[moduleId];
/******/ 					}
/******/ 				}
/******/ 				if(runtime) var result = runtime(__webpack_require__);
/******/ 			}
/******/ 			if(parentChunkLoadingFunction) parentChunkLoadingFunction(data);
/******/ 			for(;i < chunkIds.length; i++) {
/******/ 				chunkId = chunkIds[i];
/******/ 				if(__webpack_require__.o(installedChunks, chunkId) && installedChunks[chunkId]) {
/******/ 					installedChunks[chunkId][0]();
/******/ 				}
/******/ 				installedChunks[chunkId] = 0;
/******/ 			}
/******/ 			return __webpack_require__.O(result);
/******/ 		}
/******/
/******/ 		var chunkLoadingGlobal = self["webpackChunk"] = self["webpackChunk"] || [];
/******/ 		chunkLoadingGlobal.forEach(webpackJsonpCallback.bind(null, 0));
/******/ 		chunkLoadingGlobal.push = webpackJsonpCallback.bind(null, chunkLoadingGlobal.push.bind(chunkLoadingGlobal));
/******/ 	})();
/******/
/************************************************************************/
/******/
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module depends on other loaded chunks and execution need to be delayed
/******/ 	__webpack_require__.O(undefined, ["assets/css/style","assets/fontawesome/css/all-purge.min","assets/bootstrap/css/bootstrap-purge.min"], () => (__webpack_require__("./public/assets/js/scripts.js")))
/******/ 	__webpack_require__.O(undefined, ["assets/css/style","assets/fontawesome/css/all-purge.min","assets/bootstrap/css/bootstrap-purge.min"], () => (__webpack_require__("./resources/css/style.scss")))
/******/ 	__webpack_require__.O(undefined, ["assets/css/style","assets/fontawesome/css/all-purge.min","assets/bootstrap/css/bootstrap-purge.min"], () => (__webpack_require__("./public/assets/bootstrap/css/bootstrap.min.css")))
/******/ 	var __webpack_exports__ = __webpack_require__.O(undefined, ["assets/css/style","assets/fontawesome/css/all-purge.min","assets/bootstrap/css/bootstrap-purge.min"], () => (__webpack_require__("./public/assets/fontawesome/css/all.min.css")))
/******/ 	__webpack_exports__ = __webpack_require__.O(__webpack_exports__);
/******/
/******/ })()
;
